import React, {
  PropTypes,
} from 'react';

const CookieToaster = React.createClass({
  render() {
    return (
      <div>{this.props.text || 'HEYOO'}</div>
    );
  }
});

export default CookieToaster;
